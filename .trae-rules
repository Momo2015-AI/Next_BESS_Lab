# Trae AI 代码规范

你是一个专业的前端/后端开发AI。在生成代码时必须遵守以下规范：

## 前端 (Vue 3 + Vite)

### 命名
- 组件文件: PascalCase (.vue)
- 组合式函数: camelCase + use 前缀 (useDraft.js)
- Store: camelCase (bess.js)
- CSS 类: kebab-case (tool-page, step-content)
- 常量: UPPER_SNAKE_CASE

### 强制规则
1. 所有文本必须使用 $t('key') 国际化，禁止硬编码中文/英文
2. 禁止内联事件处理器 (onfocus/onblur/onmouseover/onmouseout)，改用 CSS :focus-visible 或 @focus/@blur
3. 禁止内联 style 属性，使用 CSS 类或 CSS 变量
4. 组件不超过 400 行，超了必须拆分
5. ECharts 实例必须通过 useChart() composable 管理
6. window.addEventListener('resize') 必须在 onUnmounted 中 removeEventListener
7. setInterval 必须在 onUnmounted 中 clearInterval
8. watch({ deep: true }) 必须配合防抖 (300-500ms)

### 共享样式
- 公共样式在 src/assets/styles/shared.css
- 页面布局类: .tool-page, .phase-page, .tool-header
- 输入框使用 .base-input 类
- 按钮使用 .base-btn 类
- 卡片使用 .base-card 类

### ECharts 使用方式
```javascript
import { useChart } from '@/composables/useChart'

const chartRef = ref(null)
const chartOptions = ref({ /* ECharts配置 */ })

const { chart, resize, update } = useChart(chartRef, chartOptions)
```

## 后端 (Python + Flask)

### 命名
- 模块: snake_case
- 类: PascalCase
- 函数: snake_case
- 常量: UPPER_SNAKE_CASE

### 强制规则
1. 所有外键列必须添加索引
2. 禁止 N+1 查询，使用 joinedload/selectinload
3. 列表查询必须分页
4. 批量删除+插入必须使用事务包裹

## 参考
- 完整规范: CODE_STYLE.md
- 翻译文件: src/i18n/zh.js 和 src/i18n/en.js
- 共享样式: src/assets/styles/shared.css
- ECharts Composable: src/composables/useChart.js