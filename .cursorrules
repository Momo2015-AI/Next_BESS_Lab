# 项目代码规范

你是一个专业的前端/后端开发 AI。在生成代码时必须遵守以下规范：

## 前端 (Vue 3 + Vite)

### 命名
- 组件文件: PascalCase (.vue)
- 组合式函数: camelCase + use 前缀 (useDraft.js)
- Store: camelCase (bess.js)
- CSS 类: kebab-case (tool-page, step-content)
- 常量: UPPER_SNAKE_CASE

### 强制规则
1. 所有文本必须使用 $t('key') 国际化，禁止硬编码中文/英文
2. 禁止内联事件处理器 (onfocus/onblur/onmouseover/onmouseout)，改用 CSS :focus-visible 或 @focus/@blur
3. 禁止内联 style 属性，使用 CSS 类
4. 组件不超过 400 行，超了必须拆分
5. ECharts 实例必须在 onUnmounted 中 dispose
6. window.addEventListener('resize') 必须在 onUnmounted 中 removeEventListener
7. setInterval 必须在 onUnmounted 中 clearInterval
8. MutationObserver 必须在 onUnmounted 中 disconnect
9. watch({ deep: true }) 必须配合防抖 (300-500ms)
10. 使用 import * as echarts 全量导入，改用按需导入

### 共享样式
- 公共样式提取到 src/assets/styles/shared.css
- 页面布局类: .tool-page, .phase-page, .tool-header

### 性能
- 输入框变更触发计算必须防抖 300ms+
- localStorage 写入必须防抖 500ms+

## 后端 (Python + Flask)

### 命名
- 模块: snake_case
- 类: PascalCase
- 函数: snake_case
- 常量: UPPER_SNAKE_CASE

### 强制规则
1. 所有外键列必须添加索引
2. 禁止 N+1 查询，使用 joinedload/selectinload
3. 列表查询必须分页
4. 批量删除+插入必须使用事务包裹
5. 模型 to_dict() 方法必须显式定义，禁止运行时动态注入
6. 认证端点禁止重复查询 User

## 参考
- 完整规范见 CODE_STYLE.md
- 翻译文件在 src/i18n/zh.js 和 src/i8n/en.js
