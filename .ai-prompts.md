# AI 开发 Prompt 模板

以下模板用于确保所有 AI 工具在介入项目时都遵守统一的代码规范。

---

## 通用模板（适用于所有 AI 工具）

```
你在修改 /workspace/soh-sim-frontend 和 /workspace/soh-sim-backend 项目。

请严格遵守 CODE_STYLE.md 中的所有规范，重点：
1. 所有文本必须使用 $t() 国际化，禁止硬编码中英文
2. 禁止内联事件处理器 (onfocus/onblur)，改用 CSS :focus-visible
3. 禁止内联 style 属性，使用 CSS 类
4. 组件不超过 400 行，超了必须拆分
5. 所有 ECharts 实例必须在 onUnmounted 中 dispose
6. window.addEventListener('resize') 必须有匹配的 removeEventListener
7. 所有 deep watch 必须配合防抖 (300ms+)
8. 后端所有外键列必须有索引，禁止 N+1 查询
9. 后端列表查询必须分页
10. 后端批量删除+插入必须使用事务
```

---

## 前端任务专用模板

```
你在修改 Vue 3 前端项目。

组件命名: PascalCase (.vue)
组合式函数: camelCase + use 前缀
Store: camelCase
CSS 类: kebab-case
常量: UPPER_SNAKE_CASE

修改后请运行: npm run lint
```

---

## 后端任务专用模板

```
你在修改 Flask 后端项目。

模块命名: snake_case
类名: PascalCase
函数: snake_case
常量: UPPER_SNAKE_CASE

修改后请运行: black . && isort . && flake8
```

---

## 代码审查专用模板

```
请审查以下代码变更，检查是否违反 CODE_STYLE.md 中的规范：

1. 是否有硬编码的中英文文本？
2. 是否有内联事件处理器或内联样式？
3. 组件是否超过 400 行？
4. 是否有内存泄漏风险（resize/MutationObserver/setInterval 未清理）？
5. 是否有 N+1 查询或缺少索引？
6. 是否有深 watch 无防抖？

请列出具体的违规文件和行号。
```
